{
  "name": "frontend-vite",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "node scripts/gen-version.js && vite",
    "build": "node scripts/gen-version.js && vite build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1",
    "split-history": "node scripts/split-history.js && node scripts/gen-version.js",
    "deploy": "npm run build && npx wrangler pages deploy dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^8.0.1"
  }
}
